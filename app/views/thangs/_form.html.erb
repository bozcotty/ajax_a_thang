<%= form_for(@thang), remote: true do |f| %>
  <% if @thang.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@thang.errors.count, "error") %> prohibited this thang from being saved:</h2>

      <ul>
      <% @thang.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
</div>
